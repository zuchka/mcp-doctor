# MCP Doctor v0.5 Horizon friction log

This append-only log records the deployment and product experience without erasing failed
attempts. It must never contain access tokens, private endpoint URLs, cookies,
authorization codes, or unredacted user data.

## Environment summary

- Baseline captured at: 2026-09-22T17:52:06-07:00
- Operator: repository owner with Codex
- Git branch: `codex/mcp-scope-lab`
- Baseline and rollback SHA: `08c655a9c2207ba69f0d3c0f12c9518af491ac82`
- Deploy SHA: pending final commit
- Horizon project/deployment ID: pending email verification and first deployment
- Python: 3.13.2 on Darwin arm64
- uv: 0.11.8
- FastMCP: 4.0.5
- fastmcp-tasks: 4.0.5
- httpx2: 2.13.0
- Pydantic: 2.13.5
- OpenAI SDK: 2.54.0 (local optional eval dependency; not required in Horizon)
- MCP clients: FastMCP Client 4.0.5; npm conformance runner 0.1.16
- Negotiated MCP revision against the approved demo endpoint: 2026-07-28
- Demo target type: approved public, non-sensitive documentation endpoint

## F-001: v0.4 baseline is reproducible

- Timestamp: 2026-09-22T17:40:00-07:00
- Stage: build
- Goal: establish a known-good local server and rollback point before hosted changes.
- Expected: frozen install, tests, lint, build, server import, and console entrypoint pass.
- Actual: `uv sync --frozen --extra eval` passed; 69 tests passed; Ruff passed; wheel and
  source distribution built; `mcp_doctor.server:mcp` imported. The local surface contained
  its expected nine tools and remained STDIO-oriented.
- Sanitized evidence: commands and dependency versions are recorded above; generated
  baseline artifacts were kept in a temporary directory rather than Git.
- Time spent: approximately 5 minutes.
- Manual steps: none.
- Workaround: none.
- Owner: Doctor
- Severity: observation
- Impact: reliability / interview narrative
- Proposed change or question: retain the SHA as the first rollback target.
- Verification status: verified
- Follow-up owner: Doctor

## F-002: hosted transport needs an explicit direct dependency

- Timestamp: 2026-09-22T17:43:00-07:00
- Stage: build
- Goal: construct a credential-free, no-redirect HTTP client for the remote inspector.
- Expected: use a supported FastMCP transport extension point without depending on a
  transitive package accidentally.
- Actual: FastMCP 4's `StreamableHttpTransport` accepts an `httpx_client_factory`, but its
  public type is from `httpx2`; the application therefore needs a direct `httpx2` range in
  `pyproject.toml` and a lockfile update.
- Sanitized evidence: `httpx2>=2.13,<3`; resolved version 2.13.0.
- Time spent: approximately 10 minutes.
- Manual steps: dependency and lockfile review.
- Workaround: none; declared the dependency directly.
- Owner: Doctor / FastMCP
- Severity: paper cut
- Impact: reliability
- Proposed change or question: document the factory type prominently in FastMCP transport
  examples.
- Verification status: verified
- Follow-up owner: Doctor

## F-003: the published conformance runner lags the final protocol revision

- Timestamp: 2026-09-22T17:47:00-07:00
- Stage: conformance
- Goal: run the current 2026-07-28 server requirement set against the hosted catalog.
- Expected: select the final dated requirement set with the conformance CLI.
- Actual: `@modelcontextprotocol/conformance` 0.1.16 is the latest npm release and does not
  accept the documented `--requirements` flag or contain a final 2026-07-28 suite. Its
  dated server suites stop at 2025-11-25, with separate `draft` and `extension` suites.
- Sanitized evidence: `npx -y @modelcontextprotocol/conformance --version` returned 0.1.16.
- Time spent: approximately 10 minutes.
- Manual steps: inspected CLI help, version, and npm latest metadata.
- Workaround: run the latest dated suite for diagnostic evidence only; do not present it as
  final 2026-07-28 certification.
- Owner: MCP conformance tooling
- Severity: workaround required
- Impact: reliability / interview narrative
- Proposed change or question: publish the final dated requirement set or document the
  exact draft commit that corresponds to 2026-07-28.
- Verification status: open
- Follow-up owner: deployment operator

## F-004: the full conformance command assumes a reference “everything” server

- Timestamp: 2026-09-22T17:48:00-07:00
- Stage: conformance
- Goal: distinguish protocol failures from intentionally absent optional capabilities.
- Expected: required server behaviors are separable from fixtures that require particular
  test resources, prompts, or tools.
- Actual: the 2025-11-25 run reported 11 passes and 21 failures. Core initialization, ping,
  tool listing, simple tool execution/error behavior, and related required paths passed;
  many failures expected fixture names and capabilities that MCP Doctor intentionally does
  not expose.
- Sanitized evidence: local loopback run only; no credentials or external user data.
- Time spent: approximately 10 minutes.
- Manual steps: classified failures by requirement and fixture assumption.
- Workaround: retain the run as exploratory evidence and use targeted requirements when a
  runner matching the negotiated revision is available.
- Owner: MCP conformance tooling / documentation
- Severity: paper cut
- Impact: usability / reliability
- Proposed change or question: offer a required-core profile that does not assume an
  everything-server fixture.
- Verification status: accepted limitation
- Follow-up owner: deployment operator

## F-005: direct local HTTP does not reject hostile Host and Origin values

- Timestamp: 2026-09-22T17:49:00-07:00
- Stage: security
- Goal: test the conformance runner's DNS-rebinding scenario.
- Expected: the production ingress rejects an invalid Host/Origin before MCP dispatch.
- Actual: a direct `fastmcp run` loopback server accepted the runner's hostile Host and
  Origin request with HTTP 200. This is not the supported public topology, but it proves
  that the application object alone must not be exposed as the internet ingress.
- Sanitized evidence: local endpoint only; hostile host `evil.example.com`; no token.
- Time spent: approximately 5 minutes.
- Manual steps: ran the single scenario in verbose mode.
- Workaround: expose the app only behind Horizon TLS/auth gateway; verify invalid Host and
  Origin rejection against the deployed endpoint before launch.
- Owner: Horizon / FastMCP
- Severity: blocker
- Impact: security
- Proposed change or question: confirm Horizon's host/origin enforcement and whether FastMCP
  should enable a safe trusted-host default for direct HTTP serving.
- Verification status: open
- Follow-up owner: deployment operator

## F-006: GitHub sign-in adds a separate email-code checkpoint

- Timestamp: 2026-09-22T17:57:00-07:00
- Stage: signup
- Goal: enter Horizon using the repository owner's existing GitHub identity.
- Expected: GitHub authorization returns directly to the Horizon console.
- Actual: GitHub requested read-only email access for Prefect Horizon and redirected through
  WorkOS; Horizon then required a six-digit code sent to the account email. The deployment
  flow is paused at that checkpoint for the account owner.
- Sanitized evidence: provider names and requested scope only; the address and code are not
  recorded here.
- Time spent: approximately 5 minutes so far.
- Manual steps: Horizon sign-in, GitHub OAuth authorization, email-code entry still required.
- Workaround: account owner completes the one-time verification in the open browser tab.
- Owner: Horizon / identity provider
- Severity: paper cut
- Impact: usability / interview narrative
- Proposed change or question: explain this second factor before GitHub authorization so an
  operator knows inbox access is required.
- Verification status: open
- Follow-up owner: repository owner

## F-007: hosted boundary passes local and live-target verification

- Timestamp: 2026-09-22T18:09:52-07:00
- Stage: invocation
- Goal: prove the narrowed catalog and its safety controls before pushing a deploy revision.
- Expected: all existing behavior remains green; the hosted server exposes one tool, fails
  closed without an allowlist, and inspects an approved live target without invoking a tool.
- Actual: 89 tests, Ruff, format check, package build, local and hosted imports, console help,
  secret-pattern scan, and dependency audit passed. A real in-memory call negotiated MCP
  2026-07-28, listed three target tools and one resource, reported the target's unsupported
  prompt listing as a bounded category, and made zero model or target-tool calls.
- Sanitized evidence: no known dependency vulnerabilities were reported by `pip-audit`; no
  candidate committed secret pattern was found.
- Time spent: approximately 35 minutes including an additional pre-parse HTTP body cap.
- Manual steps: reviewed the generated tool schema and bounded structured result.
- Workaround: none.
- Owner: Doctor
- Severity: observation
- Impact: security / reliability / interview narrative
- Proposed change or question: repeat the same assertions against the Horizon URL after deploy.
- Verification status: verified locally; deployed verification pending
- Follow-up owner: deployment operator

## F-008: first public-server PR had no automated checks

- Timestamp: 2026-09-22T18:12:00-07:00
- Stage: build
- Goal: make the public safety and compatibility gates repeatable on every proposed change.
- Expected: the pull request reports frozen tests, lint, format, build, and dependency audit.
- Actual: the initial PR was mergeable but had an empty check rollup because the repository
  did not contain a CI workflow.
- Sanitized evidence: GitHub reported no status checks for PR #1.
- Time spent: approximately 10 minutes.
- Manual steps: inspected PR metadata and added a least-privilege workflow.
- Workaround: added Python 3.11/3.13 CI with pinned uv, frozen dependencies, Ruff, format,
  tests, package build, and a Python 3.13 dependency audit.
- Owner: Doctor
- Severity: workaround required
- Impact: security / reliability
- Proposed change or question: require the checks before merging once they pass.
- Verification status: fixed-unverified
- Follow-up owner: repository owner

## Release blockers

- Complete Horizon email verification and create the first private preview deployment.
- Verify mandatory auth, least-privilege discovery/call behavior, and identity-aware audit.
- Verify deployed Host/Origin rejection; never expose direct `fastmcp run` HTTP publicly.
- Configure and test per-principal rate/concurrency limits and document audit retention.
- Produce a current dated conformance result once the final 2026-07-28 suite is published or
  identify a reviewed equivalent commit.
- Exercise cold start and rollback against the deployed endpoint.

## Accepted beta limitations

- One deterministic remote-inspection tool; no hosted evals, artifacts, Scope Lab, local
  paths, process execution, or downstream credentials.
- Targets are exact operator-approved HTTPS URLs on port 443 and must resolve only to global
  addresses.
- Application-level concurrency is global. Per-principal throttling is a Horizon control.
- Direct local HTTP is for loopback development only.
- The older full conformance suite is useful diagnostic evidence but not a public-launch pass.

## Interview notes

- F-002: a managed deployment exposed a real dependency/API boundary that local transitive
  imports had hidden.
- F-003/F-004: “run conformance” is not a binary checkbox when the standard and its fixtures
  are moving; record the exact runner, dated suite, and why a failure is or is not relevant.
- F-005: annotations and application-level auth are not network controls. The public security
  story is only complete when gateway behavior is tested on the real URL.
- F-006: OAuth marketing can hide a second identity-verification step that matters during a
  live onboarding or interview demo.
